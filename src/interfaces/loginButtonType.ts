export type GoogleButtonProps = {
    context: 'home' | 'other'
    text: string
    icon?: boolean
}
export type GithubButtonProps = {
    context: 'home' | 'other'
    text: string
    icon?: boolean
}